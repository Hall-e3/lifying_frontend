import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  button: {
    "& .MuiButton-root": {
      textTransform: "capitalize",
      color: "#fff",
    },
  },
}));
